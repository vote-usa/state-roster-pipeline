<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\StateBallot.Core\StateBallot.Core.csproj" />
    <ProjectReference Include="..\StateBallot.Staging\StateBallot.Staging.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- This machine only has the net10.0 runtime installed; roll forward so this
         net8.0-targeted app can still run here. Doesn't change the target. -->
    <RollForward>LatestMajor</RollForward>
  </PropertyGroup>

</Project>
